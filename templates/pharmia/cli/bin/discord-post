#!/usr/bin/env bash
# discord-post — DEPRECATED back-compat shim; posting merged into `discord post`.
# Forwards all arguments unchanged. Update callers to `discord post <channelId> ...`.
set -euo pipefail
echo "discord-post: deprecated — use 'discord post' (forwarding)" >&2
exec "$(dirname "$0")/discord" post "$@"
